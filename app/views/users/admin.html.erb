<!--  List all user's with their email addresses-->
<!--  Checkbox that turns a user into an admin-->
<!--  A form that lets the admin add new tools-->
<!--  A form that lets the admin add new news posts-->

<h2>Hello <%= @current_user.username %></h2>

<table class="table table-hover">
  <tr>
    <th>Username</th>
    <th>Email</th>
    <th>Make Admin</th>
  </tr>

  <%@users.each do |user| %>
    <tr>
      <% if user.admin == nil %>
        <td><%= user.username %></td>
        <td><%= user.email %></td>
        <td><%= user.admin %></td>
        <td><%= button_to "Make Admin", make_admin_path(user), method: :patch %></td>
    <%end %>
    </tr>

  <%end %>

</table>
